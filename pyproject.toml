[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "langchain-fastapi-production"
version = "1.0.0"
description = "Production-grade FastAPI application with LangChain integration"
license = "MIT"
authors = [{ name = "Harmeet Singh", email = "harmeetsinghfbd@gmail.com" }]
maintainers = [{ name = "Harmeet Singh", email = "harmeetsinghfbd@gmail.com" }]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Framework :: FastAPI",
    "Topic :: Internet :: WWW/HTTP :: HTTP Servers",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Topic :: AI Agents :: Libraries :: Langchain",
]
keywords = ["fastapi", "langchain", "ai", "api", "async"]
requires-python = ">=3.12,<3.14"
dependencies = [
    # --- WEB FRAMEWORK ---
    "fastapi[standard]>=0.140.0",
    "httptools>=0.7.1",
    "python-multipart>=0.0.20",
    "uvicorn[standard]>= 0.50.1",
    # --- DATA VALIDATION ---
    "pydantic>=2.11.0,<3.0.0",
    "pydantic-settings>=2.5.0,<3.0.0",
    # --- HTTP CLIENT ---
    "httpx[http2]>=0.28.1,<1.0.0",
    # --- AI/LLM FRAMEWORK ---
    "deepagents>=0.4.11",
    "langchain>=1.2.10",
    "langchain-collapse>=0.1.1",
    "langchain-cisco-aidefense>=1.0.0",
    "langchain-community>=0.4.1",
    "langchain-core>=1.2.11",
    "langchain-docling>=2.0.0",
    "langchain-google-genai>=4.2.0",
    "langchain-mcp-adapters>=0.2.2",
    "langchain-tavily>=0.2.17",
    "langchain-text-splitters>=1.1.0",
    "langgraph>=1.1.0",
    "langgraph-checkpoint-postgres>=3.0.1",
    "langsmith>=0.7.1",
    "sentence-transformers>=5.1.2",
    # --- DATABASE: POSTGRESQL ---
    "alembic==1.16.5",
    "asyncpg==0.29.0",
    "asyncpg-listen>=0.0.9",
    "pgvector>=0.4.2",
    "sqlalchemy==2.0.39",
    # --- DATABASE: MONGODB ---
    "beanie>=2.0.1",
    "motor==3.7.1",
    # --- DATABASE: NEO4J ---
    "graphiti-core[google-genai]>=0.29.1",
    "neo4j>=6.0.3",
    # --- CACHE: REDIS ---
    "redis[hiredis]>=5.1.0,<6.0.0",
    # --- MESSAGE QUEUE: CELERY + RABBITMQ ---
    "celery>=5.6.2",
    "faststream>=0.6.2",
    "flower>=2.0.1",
    # --- WEB SCRAPING ---
    "crawl4ai>=0.8.0,<1.0.0",
    "nh3>=0.3.3",
    # --- DOCUMENT PROCESSING ---
    "docling>=2.72.0",
    "langextract>=1.2.0",
    "pageindex>=0.2.8",
    # --- AUTHENTICATION ---
    "argon2-cffi>=25.1.0",
    "authlib>=1.6.9",
    "bcrypt>=5.0.0",
    "itsdangerous>=2.2.0",
    "passlib[bcrypt]>=1.7.4",
    "python-jose[cryptography]>=3.5.0",
    # --- LOGGING ---
    "loguru==0.7.3",
    # --- UTILITIES ---
    "aiofiles==24.1.0",
    "asyncer>=0.0.17",
    "asyncio>=4.0.0",
    "async-timeout>=5.0.1",
    "nanoid==2.0.0",
    "psutil==7.1.0",
    "tenacity>=9.1.2",
    # --- API FEATURES ---
    "fastapi-guard>=4.4.0",
    "fastapi-limiter>=0.2.0",
    "fastapi-pagination==0.12.1",
    # --- MONITORING ---
    "opentelemetry-api>=1.39.1",
    "opentelemetry-instrumentation-asgi==0.61b0",
    "opentelemetry-instrumentation-celery==0.61b0",
    "opentelemetry-instrumentation-httpx==0.61b0",
    "opentelemetry-instrumentation-redis==0.61b0",
    "opentelemetry-instrumentation-sqlalchemy==0.61b0",
    "opentelemetry-exporter-prometheus>=0.60b0",
    "opentelemetry-sdk>=1.39.1",
    # --- MCP ---
    "fastmcp>=3.4.2",
    # --- DEVELOPMENT & EVALUATION ---
    "apscheduler>=3.11.2",
    "cognee>=1.1.0",
    "compact-middleware>=0.1.1",
    "dspy>=2.5.7",
    "gepa>=0.1.1",
    "honcho-ai>=2.0.1",
    "openevals>=0.2.0",
    "returns>=0.26.0",
    "schemathesis",
    # --- UTILITIES & OTHER ---
    "boto3>=1.42.68",
    "botocore>=1.42.68",
    "charset-normalizer>=3.0.0",
    "fpdf2>=2.8.7",
    "gepa>=0.1.1",
    "icecream>=2.1.10",
    "pendulum>=3.2.0",
    "perfect",
    "psycopg>=3.3.3",
    "python-docx>=1.2.0",
    "resend>=2.24.0",
    "sqlmodel>=0.0.37",
    "thordata-sdk>=1.8.5",
    "toons>=0.5.4",
    "tree-sitter>=0.25.2",
    "tree-sitter-python>=0.25.0",
    "urllib3>=2.2.0",
    "langchain-openrouter>=0.2.1",
    "markitdown>=0.1.5",
    "langchain-nvidia-ai-endpoints>=1.2.1",
    "graphifyy>=0.5.5",
    "langcache>=0.12.0",
    "redisvl>=0.18.0",
    "headroom-ai[all]>=0.25.0",
    "mypy-boto3-s3>=1.43.14",
    "opentelemetry-exporter-otlp-proto-grpc>=1.40.0",
    "opentelemetry-instrumentation-asyncio==0.61b0",
    "opentelemetry-instrumentation-dbapi==0.61b0",
    "opentelemetry-instrumentation-logging==0.61b0",
    "opentelemetry-instrumentation-sqlite3==0.61b0",
    "opentelemetry-instrumentation-threading==0.61b0",
    "opentelemetry-instrumentation-urllib==0.61b0",
    "opentelemetry-instrumentation-wsgi==0.61b0",
    "opentelemetry-instrumentation-openai-v2>=2.4b0",
    "opentelemetry-instrumentation-aiohttp-client==0.61b0",
    "opentelemetry-instrumentation-aiohttp-server==0.61b0",
    "opentelemetry-instrumentation-asyncpg==0.61b0",
    "opentelemetry-instrumentation-boto3sqs==0.61b0",
    "opentelemetry-instrumentation-botocore==0.61b0",
    "opentelemetry-instrumentation-click==0.61b0",
    "opentelemetry-instrumentation-fastapi==0.61b0",
    "opentelemetry-instrumentation-grpc==0.61b0",
    "opentelemetry-instrumentation-jinja2==0.61b0",
    "opentelemetry-instrumentation-psycopg==0.61b0",
    "opentelemetry-instrumentation-pymongo==0.61b0",
    "opentelemetry-instrumentation-requests==0.61b0",
    "opentelemetry-instrumentation-starlette==0.61b0",
    "opentelemetry-instrumentation-system-metrics==0.61b0",
    "opentelemetry-instrumentation-tornado==0.61b0",
    "opentelemetry-instrumentation-tortoiseorm==0.61b0",
    "opentelemetry-instrumentation-urllib3==0.61b0",
    "llvmlite>=0.48.0",
    "numba>=0.66.0",
    "msgspec>=0.21.1",
    "autoregistry>=1.3.1",
    "dacite>=1.9.2",
    "dishka>=1.10.1",
    "geopy>=2.5.0",
    "pint>=0.25.3",
    "faker>=40.11.0",
    "zensical>=0.0.54",
]

[project.scripts]
dev = "app.server:main"
lint = "ruff:check"
format = "ruff:format"
type-check = "ty:check"
# precommit = "pre-commit:run"
migrate = "alembic:upgrade"
test = "pytest:main"
replay-outbox = "scripts.replay_outbox:main"


[project.optional-dependencies]
dev = [
    "pytest>=8.2.2,<9.0.0",
    "pytest-asyncio>=0.23.7,<1.0.0",
    "pytest-cov>=5.0.0,<6.0.0",
    "httpx>=0.27.0,<1.0.0",          # bumped slightly; use latest stable
    "pre-commit==4.3.0",
    "bandit>=1.8.6,<2.0.0",
    "safety>=3.6.2,<4.0.0",
    "ty>=0.0.56",                    # Astral type checker
]
test = [
    "pytest>=8.2.2,<9.0.0",
    "pytest-asyncio>=0.23.7,<1.0.0",
    "pytest-cov>=5.0.0,<6.0.0",
    "httpx>=0.27.0,<1.0.0",
    "factory-boy>=3.3.0,<4.0.0",
    "fakeredis[lua]>=2.24.0,<3.0.0",
]
docs = [
    "mkdocs>=1.5.0",
    "mkdocs-material>=9.4.0",
    "mkdocstrings[python]>=0.24.0",
]

[project.urls]
Homepage = "https://github.com/Harmeet10000/langchain-fastapi-production"
Documentation = "https://github.com/Harmeet10000/langchain-fastapi-production#readme"
Repository = "https://github.com/Harmeet10000/langchain-fastapi-production"
"Bug Tracker" = "https://github.com/Harmeet10000/langchain-fastapi-production/issues"

# ────────────────────────────────────────
# UV – Dependency & Lockfile Management
# ────────────────────────────────────────
[tool.uv]
default-groups = ["dev"] # uv sync installs dev tools by default
# Optional: add if you hit index issues
# index-strategy = "first-index"

# Your existing dependency-groups is perfect (modern replacement for old dev-dependencies)
[dependency-groups]
dev = ["pre-commit>=4.3.0", "ruff>=0.15.20", "ty>=0.0.56"]
test = ["pytest>=8.2.2,<9.0.0", "pytest-asyncio>=0.23.7,<1.0.0", "pytest-cov>=5.0.0,<6.0.0", "httpx>=0.27.0,<1.0.0", "factory-boy>=3.3.0,<4.0.0", "fakeredis[lua]>=2.24.0,<3.0.0"]

# ────────────────────────────────────────
# RUFF – Linting & Formatting
# ────────────────────────────────────────
[tool.ruff]
line-length = 100        # Modern choice; more room for chains
target-version = "py312"
src = ["src"]            # First-party code layout

# Exclude common directories (explicit for clarity)
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "venv",
    "*.egg-info",
]

[tool.ruff.lint]
preview = true
select = [
    # --- Core rules (existing) ---
    "E",
    "W",     # pycodestyle
    "F",     # pyflakes (unused/undefined)
    "I",     # isort
    "UP",    # pyupgrade (modern syntax)
    "B",     # bugbear (common bugs)
    "A",     # builtins shadowing
    "C4",    # comprehensions
    "PERF",  # performance hints (hot paths)
    "TRY",   # tryceratops (exceptions in async)
    "ASYNC", # async misuse (critical!)
    "RUF",   # ruff-specific
    "PL",    # pylint port (selective)
    "ANN",   # annotations (complements ty)
    "S",     # bandit (security)
    "SIM",   # simplify
    "PTH",   # pathlib over os.path
    "TCH",   # type-checking imports
    "RET",   # return statements
    "ARG",   # unused arguments
    # --- High-value additions ---
    "LOG",  # logging best practices (complements loguru usage)
    "FAST", # FastAPI-specific rules (redundant response_model, missing Annotated)
    "PT",   # pytest best practices
    "T20",  # print statements (catch debug prints)
    "DTZ",  # datetime/timezone (UTC best practices)
    "BLE",  # blind except (prevents catching generic exceptions)
    # --- Medium-value additions ---
    "PIE",  # flake8-pie (various micro-optimizations)
    "ICN",  # import conventions (consistency)
    "FURB", # modern Python patterns & refurb suggestions
    # --- New: blanket suppression + debt tracking ---
    "PGH",  # blanket /type-ignore (PGH003, PGH004)
    "FIX",  # FIXME/XXX/HACK tracking (complements TD)
    "T10",  # debugger left in code (T100)
    # --- High-value for production ---
    "N",   # pep8-naming (class/function/variable naming consistency)
    "SLF", # private member access (enforces encapsulation)
    "EM",  # exception message formatting (best practices)
    "TD",  # TODO/FIXME tracking (technical debt management)
    # --- Additional value-adds ---
    "COM", # trailing comma enforcement (API signatures)
    "FBT", # boolean trap (clarity on bool parameters)
    "G",   # logging format validation (complements LOG)
    "RSE", # exception raising patterns
    "INP", # implicit namespace packages (package structure)
    "PLW", # pylint warnings (for better code quality, e.g., PLW3201 bad-dunder-method)
]

# Enable autofix for safe rules only
fixable = [
    "I",    # isort (import sorting)
    "F401", # unused imports
    "UP",   # pyupgrade (syntax modernization)
    "C4",   # comprehensions
    "SIM",  # simplifications
    "PTH",  # pathlib
    "RUF",  # ruff-specific
    "TC",   # type-checking imports
    # --- New rules with safe fixes ---
    "LOG002",  # Fix invalid logger.getLogger() argument
    "FAST001", # Remove redundant response_model
    "FAST002", # Add missing Annotated
    "T201",    # Remove print() calls
    "PIE790",  # Remove unnecessary pass
    "ICN001",  # Fix import alias conventions
    "PT001",   # Fix pytest.fixture parentheses
    "PT003",   # Remove scope='function' (implied)
    # --- Additional safe fixes ---
    "N803",   # Fix invalid argument names to lowercase
    "N804",   # Fix invalid classmethod first argument to cls
    "N805",   # Fix invalid method first argument to self
    "COM812", # Add missing trailing commas
    "EM101",  # Fix raw string in exception
    "EM102",  # Fix f-string in exception
    "EM103",  # Fix .format() in exception
    "RSE102", # Remove unnecessary parentheses on raise
    "FBT001", # Signal boolean parameter trap (for awareness)
    "PGH004", # Fix blanket  → specific rule codes
    "RUF068", # Fix duplicate entries in __all__
    "PLR6104", # Fix non-augmented assignment to augmented
]
unfixable = [
    "B",   # bugbear (needs review)
    "ANN", # annotations (manual decision)
    "S",   # security (needs review)
]
ignore = [
    "E501",    # line-too-long → formatter handles
    "ANN401",  # Any type (LangChain heavy)
    "ISC001",  # conflicting with formatter
    "TRY003",  # raise from e – sometimes verbose
    "PLR0913", # too-many-arguments (agents/graphs)
    "PLR2004", # magic values (AI logic)
    "PLR0911", # too-many-returns (graph flows)
    "ANN001",  # Missing type annotation (too noisy in AI code)
    "ANN002",  # Missing type annotation for function/method (too noisy in AI code)
    "ANN003",  # Missing type annotation for *args (too noisy in AI code)
    "ANN204",  # Missing type annotation for self (common in FastAPI)
    # --- New rule ignores ---
    "LOG001",  # Direct logger instantiation (loguru is a wrapper)
    "PT011",   # pytest.raises too broad (noisy, covered by tests)
    "FURB101", # Path.read_whole_file (too opinionated)
    "TD002",   # Missing TODO author (noisy for rapid development)
    "TD003",   # Missing TODO issue link (too strict for spikes)
    "FBT001",  # Boolean positional argument (noisy, but caught by rule)
    "FBT002",  # Boolean default value (noisy, but caught by rule)
    "FBT003",  # Boolean positional value in call (noisy)
    "COM812",  # Trailing comma missing (conflicts with formatter in some cases)
    "SLF001",  # Private member access (occasionally needed in tests)
    "RUF029",  # unused-async: FastAPI deps are async by convention
    "PLW0717", # too-many-statements-in-try-clause (orchestration/agent code)
    "PLW3201", # bad-dunder-method: SQLAlchemy declared_attr.directive makes __tablename__ a special pattern
]

[tool.ruff.lint.flake8-type-checking]
strict = true
exempt-modules = ["pydantic", "fastapi", "langchain", "langgraph", "docling"]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query", "pydantic.Field"]

[tool.ruff.lint.isort]
known-first-party = ["src"]
known-third-party = ["fastapi", "pydantic", "sqlalchemy", "langchain"]
section-order = [
    "future",
    "standard-library",
    "third-party",
    "first-party",
    "local-folder",
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = [
    "ANN",     # No type annotations required in tests
    "D",       # No docstrings required in tests
    "S101",    # Allow assert statements
    "PLR2004", # Allow magic values
    "ARG001",  # Allow unused arguments
    "E501",    # Allow long lines
    "T201",    # Allow print() in tests
    "PT004",   # Fixture without leading underscore
    "PT005",   # Fixture returns value
    "SLF001",  # Private member access allowed in tests
    "N802",    # Allow invalid function names in tests
    "N806",    # Allow invalid variable names in test fixtures
    "T100",    # Allow debugger calls in test code
]
"src/alembic/versions/*.py" = [
    "ANN",
    "N999", # Allow invalid module names
    "E501", # Allow long lines
    "F401", # Allow unused imports
]
"**/__init__.py" = [
    "ANN",  # No type annotations required in __init__.py
    "F401", # Allow unused imports (re-exports)
    "RUF067", # Non-empty __init__.py is intentional for re-exports
    # "E402",      # Allow imports not at top
]
"**/generated/**.py" = [
    "ANN",  # No type annotations required in generated code
    "E402", # Allow imports not at top
    "F841", # Allow unused variables
]
"src/app/examples/*.py" = [
    "ARG001",
    "ARG002",
    "ASYNC109",
    "ASYNC230",
    "ASYNC240",
    "ASYNC250",
    "B007",
    "B904",
    "BLE001",
    "E722",
    "FAST002",
    "PLW2901",
    "RET503",
    "RET504",
    "RET505",
    "S105",
    "S106",
    "S108",
    "S112",
    "SIM102",
    "TRY201",
    "TRY300",
    "TRY301",
    "TRY400",
    "T100",    # Allow debugger calls in examples
]
"src/app/shared/rag/rag_agent_advanced.py" = [
    "ANN201",
    "ARG001",
    "ASYNC250",
    "B007",
    "BLE001",
    "F821",
    "PLC0415",
]
"src/app/shared/rag/document_processing/ingest.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/shared/rag/document_processing/ingest_v2.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/shared/rag/document_processing/embedder.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/shared/rag/document_processing/docling_enhanced.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/shared/rag/document_processing/chunker.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/shared/rag/document_processing/entity_extractor.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/shared/otel/instrument.py" = [
    "PLC0415", # Late imports conditional on instrumentation availability
]
"src/app/shared/otel/__init__.py" = [
    "PLC0415", # Late imports conditional on instrumentation availability
]
"src/app/shared/langchain_layer/callback.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/shared/langchain_layer/chains.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/shared/langchain_layer/agents/middlewares/guardrails.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/shared/langchain_layer/agents/tools/crawl.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/lifecycle/lifespan.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/features/search/service.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/features/documents/service.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/middleware/server_middleware.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/app/connections/crawl4ai.py" = [
    "PLC0415", # Late imports to break circular deps
]
"src/mcp_core/testing.py" = [
    "PLC0415", # Late imports to avoid startup overhead
]
"src/app/shared/langchain_layer/agents/tools/shell.py" = [
    "ASYNC240", # Blocking path methods in async — uses os.path intentionally
    "PLC0415",  # Local import re to avoid name collision
]
"**/_*.py" = ["ANN201"] # Private modules
"src/app/features/auth/router.py" = ["S105"]
"src/app/features/auth/security.py" = ["S105", "S106"]
"src/app/features/auth/service.py" = [
    "PLC0415", # Late imports to break circular deps
    "S105",
    "S106",
]
"src/app/features/crawler/dependencies.py" = ["PLC0415"]
"src/mcp_core/server/*.py" = ["PLC0415"]
"src/mcp_core/__init__.py" = ["F822", "PLC0415"] # lazy __getattr__ exports; imports inside __getattr__


[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
# preview = true

# ────────────────────────────────────────
# TY – Type Checker (Astral)
# ────────────────────────────────────────
[tool.ty]
# Inferred from requires-python, but be explicit
[tool.ty.environment]
python-version = "3.12"
root = ["src"]          # Matches your src layout



[tool.ty.rules]
# --- Runtime error prevention (error level) ---
unresolved-import = "error"
unresolved-reference = "error"
invalid-assignment = "error"

# --- Async correctness (error level, critical for FastAPI + asyncpg/motor) ---
unused-awaitable = "warn"                 # Catch forgotten awaits

# --- Function call safety (error level) ---
invalid-argument-type = "error"
invalid-return-type = "error"
call-non-callable = "error"
missing-argument = "error"
no-matching-overload = "error"
unknown-argument = "error"
too-many-positional-arguments = "error"

# --- Type system integrity (error level) ---
invalid-method-override = "error"  # Liskov Substitution Principle
invalid-exception-caught = "error"
invalid-raise = "error"

# --- Collection/subscription safety (error level) ---
not-iterable = "error"
not-subscriptable = "error"
invalid-key = "error"
missing-typed-dict-key = "error"

# --- Code quality (error level) ---
empty-body = "error" # Catch incomplete implementations

# --- Critical class/function definitions (error level) ---
conflicting-declarations = "error"  # Variable declared as two conflicting types
conflicting-metaclass = "error"     # Metaclass conflicts (runtime error)
duplicate-base = "error"            # Duplicate base classes (runtime error)
invalid-context-manager = "error"   # with statement protocol violations
invalid-parameter-default = "error" # Default values not assignable to param type
dataclass-field-order = "error"     # Required fields before optional fields
invalid-dataclass = "error"         # @dataclass on NamedTuple/TypedDict/Enum/Protocol

# --- Pydantic v2 and magic attribute handling (warn level, allows flexibility) ---
unresolved-attribute = "warn"
redundant-cast = "warn"
unused-ignore-comment = "warn"
unsupported-dynamic-base = "warn"
possibly-missing-attribute = "error"
index-out-of-bounds = "warn"
possibly-missing-import = "error"
unresolved-global = "warn"           # Global variable binding issues
redundant-final-classvar = "warn"    # Final + ClassVar redundancy detection

# --- Optional: Nice-to-have patterns (warn level) ---
deprecated = "warn"                # Track library deprecations
unsupported-operator = "warn"      # Prevent unsupported ops
subclass-of-final-class = "warn"   # If you use @final decorator
ambiguous-protocol-member = "warn" # If you use Protocol
override-of-final-method = "warn"  # If you use @final methods
invalid-explicit-override = "warn" # @override without parent method (PEP 698)

# --- NEW: High-impact rules (error level) ---
missing-override-decorator = "error"      # Require @override on all overrides (catches silent breakage)
missing-type-argument = "error"           # Require explicit type args on generics (list[str] not list)
shadowed-type-variable = "error"          # Prevent PEP 695 type var shadowing in nested generics
invalid-match-pattern = "error"           # Validate match/case patterns at compile time (heavy Result usage)
isinstance-against-protocol = "error"     # Prevent isinstance(x, Protocol) runtime TypeError
isinstance-against-typed-dict = "error"   # Prevent isinstance(x, TypedDict) runtime TypeError
unbound-type-variable = "error"           # Catch type vars used outside generic scope
parameter-already-assigned = "error"      # Catch duplicate keyword args (func(x=1, x=2))
positional-only-parameter-as-kwarg = "error"  # Catch kwarg on positional-only (def f(x, /): f(x=1))

# --- NEW: Medium-impact rules (warn level) ---
mismatched-type-name = "warn"             # TypeVar("U") assigned to T, TypedDict("Film") to Movie
experimental-syntax = "warn"              # Flag A & B, ~A intersections/negations (non-standard)
subclass-of-dataclass-with-order = "warn" # Guard LSP violation with @dataclass(order=True)
useless-overload-body = "warn"            # @overload bodies are dead code
undefined-reveal = "warn"                 # reveal_type() without import

# --- NEW: Guardrail rules (error level, near-zero false positives) ---
cyclic-type-alias-definition = "error"
cyclic-class-definition = "error"
invalid-type-variable-bound = "error"
invalid-type-variable-constraints = "error"
invalid-type-variable-default = "error"
invalid-type-alias-type = "error"
invalid-type-arguments = "error"
invalid-newtype = "error"
invalid-legacy-positional-parameter = "warn"
invalid-legacy-type-variable = "error"
invalid-metaclass = "error"
invalid-named-tuple = "error"
invalid-type-checking-constant = "error"
invalid-type-guard-call = "error"
invalid-type-guard-definition = "error"
non-callable-init-subclass = "error"
override-of-final-variable = "error"
unavailable-implicit-super-arguments = "error"
unsupported-base = "warn"
unsupported-bool-conversion = "error"
static-assert-error = "error"

# --- Additional guardrails (error level) ---
call-abstract-method = "error"
invalid-overload = "error"
invalid-yield = "error"
invalid-typed-dict-field = "error"
invalid-typed-dict-header = "error"
invalid-typed-dict-statement = "error"
abstract-method-in-final-class = "error"
invalid-super-argument = "error"
invalid-paramspec = "error"
invalid-generic-class = "error"
invalid-generic-enum = "error"
invalid-frozen-dataclass-subclass = "error"
duplicate-kw-only = "error"

# --- Quality of life (warn level) ---
ineffective-final = "warn"           # Catch final(type(...)) misuse

[tool.ty.terminal]
output-format = "full"

[tool.ty.src]
# Remove `build` from the excluded directories.
# Exclude common directories (explicit for clarity)
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "src/alembic",
    "src/app/examples",
    "src/app/shared/rag",  # ponytail: vendored LangChain internals, not our code
    "venv",
    "*.egg-info",
]

# ────────────────────────────────────────
# Other sections unchanged (alembic, pytest, coverage, bandit)
# ────────────────────────────────────────

[tool.alembic]
script_location = "src/alembic"
version_locations = "src/alembic/versions"

[[tool.alembic.post_write_hooks]]
name = "ruff-format"
type = "exec"
executable = "ruff"
options = "format REVISION_SCRIPT_FILENAME"

[[tool.alembic.post_write_hooks]]
name = "ruff-fix"
type = "exec"
executable = "ruff"
options = "check --fix REVISION_SCRIPT_FILENAME"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["*.txt", "*.md", "*.yml", "*.yaml", "*.json"]

[tool.pytest.ini_options]
minversion = "7.0"
addopts = [
    "--strict-markers",
    "--strict-config",
    "-m",
    "not integration",
]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "unit: marks tests as unit tests",
]

[tool.coverage.run]
source = ["src"]
omit = [
    "*/tests/*",
    "*/test_*",
    "*/__pycache__/*",
    "*/migrations/*",
    "*/venv/*",
    "*/.venv/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug:",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]

[tool.bandit]
exclude_dirs = ["tests", "migrations"]
skips = ["B101", "B601"]

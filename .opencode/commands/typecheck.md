---
description: Run ty type checker on src/
---

Run `uv run ty check src/` and report any type errors found.

Display:
- Number and type of errors/warnings
- A one-line verdict: "✅ All types clean" or "❌ N type errors remaining"

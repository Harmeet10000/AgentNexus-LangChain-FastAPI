id: no-new-apperror-subclass
language: python
severity: error
message: AppError hierarchy is retired — use a closed FeatureError union owned by the classifier
note: |
  The migration is complete. AppError and every subclass are forbidden; new
  failures belong to a classifier-owned closed FeatureError union.
rule:
  kind: class_definition
  regex: '\(AppError'

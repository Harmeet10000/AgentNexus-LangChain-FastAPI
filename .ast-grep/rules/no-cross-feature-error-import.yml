id: no-cross-feature-error-import
language: python
severity: error
message: Feature must not import another feature's error types or code enum
note: |
  feature-error-contract — each feature owns its errors.py. Where two
  features exchange a failure, the caller translates into its own union.
rule:
  pattern: from app.features.$OTHER.errors import $ERR

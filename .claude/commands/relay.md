Run the four-leg relay on the task below.

Follow the `relay` skill at `.claude/skills/relay/SKILL.md` — read it now if it is not already loaded, then orchestrate: scout → planner → (you implement) → verifier → anchor.

You dispatch every leg and hold the baton throughout. Never run a leg yourself.

Task: $ARGUMENTS

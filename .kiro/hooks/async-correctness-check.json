{
    "name": "Async Correctness Check",
    "version": "1.0.0",
    "description": "Verify async/await patterns and I/O operations",
    "when": {
        "type": "fileEdited",
        "patterns": [
            "src/app/**/*.py"
        ]
    },
    "then": {
        "type": "askAgent",
        "prompt": "Check this file for async correctness: all I/O must be async, use async clients (motor, asyncpg, redis.asyncio), no blocking sync code in async functions, proper use of asyncio.gather vs asyncio.Queue, correct await patterns. Flag any sync I/O or blocking operations."
    }
}
